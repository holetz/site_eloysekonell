# Preflight for site_eloysekonell

Working directory: `c:\Users\eloys\Repos\site_eloysekonell`

This project has NO lint, typecheck, or test commands configured (see READBEFORE §2).

## Preflight command

```bash
cd c:/Users/eloys/Repos/site_eloysekonell && npm run build
```

The build MUST exit with code 0 (green) for the task to be considered complete.

## Notes
- Build output goes to `docs/` (required for GitHub Pages — do NOT change outDir).
- If the build fails due to type errors, missing imports, or content schema violations, fix them before marking the task complete.
- Smoke visual testing (opening the preview in a browser) is OPTIONAL for quick-inspector but REQUIRED for deep-inspector on UI tasks.
